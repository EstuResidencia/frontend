import 'package:flutter/material.dart';

class Step4PasswordView extends StatelessWidget {
  const Step4PasswordView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Step4PasswordView');
  }
}
