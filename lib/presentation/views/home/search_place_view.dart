import 'package:flutter/material.dart';

class SearchPlaceView extends StatelessWidget {
  const SearchPlaceView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('SearchPlaceView');
  }
}
